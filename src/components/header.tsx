import React from 'react';
import logo from '../icons/Fundo.png';

export default () => {
  return (
    <header>
        <div>
          <img src={logo} alt="logo" />
        </div>
    </header>
  );
}